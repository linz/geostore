# Configuration file for pre-commit (https://pre-commit.com/)

repos:
    - repo: local
      hooks:
          - id: gitlint
            name: gitlint
            entry: gitlint
            args: ["--msg-filename"]
            language: system
            stages: [commit-msg]
